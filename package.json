{
  "name": "aavegotchi-vault-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ aavegotchi/gotchi-vault",
    "create-local": "graph create --node http://localhost:8020/ aavegotchi/gotchi-vault",
    "remove-local": "graph remove --node http://localhost:8020/ aavegotchi/gotchi-vault",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aavegotchi/gotchi-vault"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.97.0",
    "@graphprotocol/graph-ts": "^0.38.0"
  }
}
